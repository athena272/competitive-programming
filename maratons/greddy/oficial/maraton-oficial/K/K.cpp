#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{
    string mov_mestre, mov_aluno;
    getline(cin, mov_mestre);
    cin.ignore();
    getline(cin, mov_aluno);

    ll erros = 0;
    for (ll i = 0; i < mov_mestre.size(); i++)
    {
        for (ll j = 0; i < mov_aluno.size(); i++)
        {
            while (erros <= 1 )
            {
                /* code */
            }
            
        }
        
    }
    
    return 0;
}