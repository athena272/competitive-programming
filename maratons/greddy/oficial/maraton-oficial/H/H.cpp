#include <bits/stdc++.h>
#include <algorithm>
#define ll long long


using namespace std;

int main()
{
    ll num_bits, num;
    cin >> num_bits >> num;

    vector<ll>(num_bits, 0);
    for (ll i = 0; i < num_bits; i++)
    {
        if(num >= )
    }
    
    return 0;
}