#include <bits/stdc++.h>
#define ll long long
#define vll vector<ll>

using namespace std;

int main()
{
    ll num_artesoes,lembrancinhas;
    cin >> num_artesoes >> lembrancinhas;
    vll artesoes(num_artesoes);
    vll tempo_acabar(num_artesoes);
    for (ll i = 0; i < num_artesoes; i++)
    {
        cin >> artesoes[i];
    }
    
    for (ll i = 0; i < num_artesoes; i++)
    {
        artesoes[i] = 
    }
    
    cout << fibo(num) << endl;
    return 0;
}